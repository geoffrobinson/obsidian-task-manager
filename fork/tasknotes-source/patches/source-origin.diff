diff --git a/src/bases/TaskListView.ts b/src/bases/TaskListView.ts
index aa66a4a..5e94ea5 100644
--- a/src/bases/TaskListView.ts
+++ b/src/bases/TaskListView.ts
@@ -2750,6 +2750,21 @@ export class TaskListView extends BasesViewBase {
 	}
 
 	private async executeSingleClickAction(task: TaskInfo, event: MouseEvent): Promise<void> {
+		if (event.altKey) {
+			const { getTaskSourceOrigin, openTaskSourceOrigin } = await import("../utils/sourceOrigin");
+			if (!getTaskSourceOrigin(task)) {
+				new Notice(this.plugin.i18n.translate("ui.taskCard.sourceBadgeMissing"));
+				return;
+			}
+			const opened = await openTaskSourceOrigin(this.plugin, task, {
+				newLeaf: event.ctrlKey || event.metaKey,
+			});
+			if (!opened) {
+				new Notice(this.plugin.i18n.translate("ui.taskCard.sourceBadgeMissing"));
+			}
+			return;
+		}
+
 		if (event.ctrlKey || event.metaKey) {
 			this.openTaskNote(task, true);
 			return;
diff --git a/src/bases/helpers.ts b/src/bases/helpers.ts
index 99fdfc4..a00cc1a 100644
--- a/src/bases/helpers.ts
+++ b/src/bases/helpers.ts
@@ -12,6 +12,7 @@ import { normalizeDependencyList } from "../utils/dependencyUtils";
 import { stringifyUnknown, stringifyUnknownArray } from "../utils/stringUtils";
 import { createTaskNotesLogger } from "../utils/tasknotesLogger";
 import { createElementInDocument } from "../utils/documentDom";
+import { hydrateTaskSourceOrigin } from "../utils/sourceOrigin";
 
 const tasknotesLogger = createTaskNotesLogger({ tag: "Bases/Helpers" });
 
@@ -233,7 +234,7 @@ function createTaskInfoFromProperties(
 		isBlocked = Array.isArray(props.blockedBy) && props.blockedBy.length > 0;
 	}
 
-	return {
+	const taskInfo: TaskInfo = {
 		title:
 			toOptionalString(props.title) ||
 			basesItem.name ||
@@ -281,6 +282,8 @@ function createTaskInfoFromProperties(
 		customProperties: Object.keys(customProperties).length > 0 ? customProperties : undefined,
 		basesData: basesItem.basesData,
 	};
+
+	return hydrateTaskSourceOrigin(taskInfo, props);
 }
 
 function mergeCustomProperties(
@@ -303,6 +306,8 @@ function enrichTaskInfoFromCache(taskInfo: TaskInfo, plugin?: TaskNotesPlugin):
 	return {
 		...cachedTask,
 		basesData: taskInfo.basesData,
+		sourceNote: taskInfo.sourceNote ?? cachedTask.sourceNote,
+		sourceLine: taskInfo.sourceLine ?? cachedTask.sourceLine,
 		customProperties: mergeCustomProperties(
 			cachedTask.customProperties,
 			taskInfo.customProperties
diff --git a/src/commands/taskNotesCommands.ts b/src/commands/taskNotesCommands.ts
index 152f3f6..866a52f 100644
--- a/src/commands/taskNotesCommands.ts
+++ b/src/commands/taskNotesCommands.ts
@@ -153,6 +153,13 @@ export function createTaskNotesCommandDefinitions(
 				await ctx.openTaskEditModalForCurrentTask();
 			},
 		},
+		{
+			id: "open-current-task-source-note",
+			nameKey: "commands.openCurrentTaskSourceNote",
+			callback: async (ctx) => {
+				await ctx.openSourceNoteForCurrentTask();
+			},
+		},
 		{
 			id: "cycle-current-task-status",
 			nameKey: "commands.cycleCurrentTaskStatus",
diff --git a/src/components/TaskContextMenu.ts b/src/components/TaskContextMenu.ts
index f50dfed..106c1ec 100644
--- a/src/components/TaskContextMenu.ts
+++ b/src/components/TaskContextMenu.ts
@@ -53,6 +53,7 @@ import {
 } from "../ui/occurrenceNoteActions";
 import { createTaskNotesLogger } from "../utils/tasknotesLogger";
 import type { UserMappedField } from "../types/settings";
+import { getTaskSourceOrigin, openTaskSourceOrigin } from "../utils/sourceOrigin";
 
 const tasknotesLogger = createTaskNotesLogger({ tag: "Components/TaskContextMenu" });
 
@@ -503,6 +504,16 @@ export class TaskContextMenu {
 			});
 		});
 
+		if (getTaskSourceOrigin(task)) {
+			this.menu.addItem((item) => {
+				item.setTitle(this.t("contextMenus.task.openSourceNote"));
+				item.setIcon("file-symlink");
+				item.onClick(() => {
+					void openTaskSourceOrigin(plugin, task);
+				});
+			});
+		}
+
 		// Copy Task Title
 		this.menu.addItem((item) => {
 			item.setTitle(this.t("contextMenus.task.copyTitle"));
diff --git a/src/i18n/resources/en.ts b/src/i18n/resources/en.ts
index 9a3a886..2fc78e5 100644
--- a/src/i18n/resources/en.ts
+++ b/src/i18n/resources/en.ts
@@ -983,7 +983,8 @@ export const en: TranslationTree = {
 			},
 			taskInteraction: {
 				header: "Task interaction",
-				description: "Configure how clicking on tasks behaves.",
+				description:
+					"Configure how clicking on tasks behaves. Alt+Click always opens the originating source note when one is stored (independent of these settings).",
 				singleClick: {
 					name: "Single-click action",
 					description: "Action performed when single-clicking a task card",
@@ -2418,6 +2419,7 @@ export const en: TranslationTree = {
 		quickActionsCurrentTask: "Quick actions for current task",
 		quickActionsTaskUnderCursor: "Quick actions for task under cursor",
 		editCurrentTask: "Edit current task",
+		openCurrentTaskSourceNote: "Open current task source note",
 		cycleCurrentTaskStatus: "Cycle current task status",
 		cycleCurrentTaskPriority: "Cycle current task priority",
 		addProjectToCurrentTask: "Add project to current task",
@@ -2969,6 +2971,7 @@ export const en: TranslationTree = {
 			unarchive: "Unarchive",
 			openNote: "Open note",
 			openNoteInNewTab: "Open note in new tab",
+			openSourceNote: "Open source note",
 			copyTitle: "Copy task title",
 			quickActions: "Quick actions",
 			noteActions: "Note actions",
@@ -3304,6 +3307,10 @@ export const en: TranslationTree = {
 			blockingBadge: "Blocking",
 			blockingBadgeTooltip: "This task is blocking another task",
 			blockingToggle: "Blocking {count} tasks",
+			sourceBadge: "Source",
+			sourceBadgeAria: "Open source note {name}",
+			sourceBadgeTooltip: "Open originating note: {path} (Alt+Click on card also works)",
+			sourceBadgeMissing: "No source note stored for this task",
 			priorityAriaLabel: "Priority: {label}",
 			taskOptions: "Task options",
 			recurrenceTooltip: "{label}: {value}",
diff --git a/src/main.ts b/src/main.ts
index ec77212..3cad6b7 100644
--- a/src/main.ts
+++ b/src/main.ts
@@ -1746,6 +1746,25 @@ export default class TaskNotesPlugin extends Plugin {
 		await this.openTaskEditModalForFile(activeFile, "Current file is not a tasknote");
 	}
 
+	async openSourceNoteForCurrentTask(): Promise<void> {
+		const task = await this.getCurrentTaskForCommand();
+		if (!task) {
+			return;
+		}
+
+		const { getTaskSourceOrigin, openTaskSourceOrigin } = await import("./utils/sourceOrigin");
+		if (!getTaskSourceOrigin(task)) {
+			new Notice(this.i18n.translate("ui.taskCard.sourceBadgeMissing"));
+			return;
+		}
+
+		const opened = await openTaskSourceOrigin(this, task);
+		if (!opened) {
+			new Notice(this.i18n.translate("ui.taskCard.sourceBadgeMissing"));
+		}
+	}
+
+
 	private currentTaskTimeTrackingPending = false;
 
 	async setCurrentTaskTimeTracking(action: "start" | "stop"): Promise<void> {
diff --git a/src/services/InstantTaskConvertService.ts b/src/services/InstantTaskConvertService.ts
index 6df4048..ae9e832 100644
--- a/src/services/InstantTaskConvertService.ts
+++ b/src/services/InstantTaskConvertService.ts
@@ -802,6 +802,14 @@ export class InstantTaskConvertService {
 		const customFrontmatter: Record<string, unknown> = {
 			...(parsedData.customFrontmatter || {}),
 		};
+
+		// Always record the originating note + line for back-navigation from Task View
+		if (currentFile) {
+			customFrontmatter.source = currentFile.path;
+			if (typeof sourceLineNumber === "number" && sourceLineNumber >= 0) {
+				customFrontmatter.source_line = sourceLineNumber;
+			}
+		}
 		if (parsedData.userFields) {
 			for (const [fieldId, value] of Object.entries(parsedData.userFields)) {
 				// Find the user field definition to get the frontmatter key
diff --git a/src/types.ts b/src/types.ts
index 4ce5ed3..d94be65 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -496,6 +496,10 @@ export interface TaskInfo {
 	hasSubtasks?: boolean; // True if another task references this task as a project
 	details?: string; // Optional task body content
 	sortOrder?: string; // LexoRank string for ordering within column
+	/** Vault path or wikilink of the note this task was converted from */
+	sourceNote?: string;
+	/** 0-based line number in the source note at conversion time */
+	sourceLine?: number;
 }
 
 export interface TaskCreationData extends Partial<TaskInfo> {
diff --git a/src/ui/TaskCard.ts b/src/ui/TaskCard.ts
index 028d7f8..1fd2feb 100644
--- a/src/ui/TaskCard.ts
+++ b/src/ui/TaskCard.ts
@@ -268,7 +268,9 @@ export function createTaskCard(
 				}),
 		});
 
-	card.addEventListener("click", clickHandler);
+	// Capture so Source/status controls can cancel a pending single-click timer
+	// even when they stopPropagation on the bubble phase.
+	card.addEventListener("click", clickHandler, true);
 	card.addEventListener("auxclick", auxclickHandler);
 	card.addEventListener("dblclick", dblclickHandler);
 	card.addEventListener("contextmenu", contextmenuHandler);
diff --git a/src/ui/taskCardMetadata.ts b/src/ui/taskCardMetadata.ts
index 16eb9a2..9448415 100644
--- a/src/ui/taskCardMetadata.ts
+++ b/src/ui/taskCardMetadata.ts
@@ -4,6 +4,11 @@ import type { TaskInfo } from "../types";
 import { formatDateTimeForDisplay } from "../utils/dateUtils";
 import { parseLinkToPath } from "../utils/linkUtils";
 import { convertInternalToUserProperties, isPropertyForField } from "../utils/propertyMapping";
+import {
+	getSourceDisplayName,
+	getTaskSourceOrigin,
+	openTaskSourceOrigin,
+} from "../utils/sourceOrigin";
 import {
 	getDefaultVisibleProperties,
 	renderPropertyMetadata,
@@ -153,6 +158,51 @@ function createOccurrenceMetadataPill(config: RenderTaskCardMetadataConfig): HTM
 	return pill;
 }
 
+function createSourceMetadataPill(config: RenderTaskCardMetadataConfig): HTMLElement | null {
+	const { metadataLine, task, plugin } = config;
+	const origin = getTaskSourceOrigin(task);
+	if (!origin) {
+		return null;
+	}
+
+	const displayName = getSourceDisplayName(origin.source);
+	const sourceLabel = plugin.i18n.translate("ui.taskCard.sourceBadge");
+	const pill = metadataLine.createSpan({
+		cls: "task-card__metadata-pill task-card__metadata-pill--source",
+		attr: {
+			"data-tn-click-exclude": "true",
+		},
+	});
+	setIcon(pill, "file-symlink");
+	pill.createSpan({ text: `${sourceLabel}: ${displayName}` });
+	prepareInteractiveControl(pill);
+	pill.setAttribute(
+		"aria-label",
+		plugin.i18n.translate("ui.taskCard.sourceBadgeAria", { name: displayName })
+	);
+	setTooltip(
+		pill,
+		plugin.i18n.translate("ui.taskCard.sourceBadgeTooltip", {
+			path: origin.source,
+		}),
+		{ placement: "top" }
+	);
+	pill.addEventListener("click", (event) => {
+		event.preventDefault();
+		event.stopPropagation();
+		void openTaskSourceOrigin(plugin, task);
+	});
+	pill.addEventListener("auxclick", (event) => {
+		event.preventDefault();
+		event.stopPropagation();
+	});
+	pill.addEventListener("dblclick", (event) => {
+		event.preventDefault();
+		event.stopPropagation();
+	});
+	return pill;
+}
+
 export function renderTaskCardMetadata(config: RenderTaskCardMetadataConfig): HTMLElement[] {
 	const { metadataLine, task, plugin, visibleProperties, propertyOptions = {} } = config;
 	metadataLine.empty();
@@ -164,6 +214,11 @@ export function renderTaskCardMetadata(config: RenderTaskCardMetadataConfig): HT
 		metadataElements.push(occurrencePill);
 	}
 
+	const sourcePill = createSourceMetadataPill(config);
+	if (sourcePill) {
+		metadataElements.push(sourcePill);
+	}
+
 	for (const propertyId of propertiesToShow) {
 		if (
 			isPropertyForField(propertyId, "status", plugin) ||
diff --git a/src/utils/TaskManager.ts b/src/utils/TaskManager.ts
index d89d52d..b3f4ac5 100644
--- a/src/utils/TaskManager.ts
+++ b/src/utils/TaskManager.ts
@@ -348,12 +348,18 @@ export class TaskManager extends Events {
 				isBlocked = Array.isArray(mappedTask.blockedBy) && mappedTask.blockedBy.length > 0;
 			}
 
+			const frontmatterRecord =
+				frontmatter !== null && typeof frontmatter === "object" && !Array.isArray(frontmatter)
+					? (frontmatter as Record<string, unknown>)
+					: null;
+
 			return buildTaskInfoFromMappedTask({
 				path,
 				mappedTask,
 				defaultTaskStatus: this.settings.defaultTaskStatus,
 				isBlocked,
 				blockingTasks,
+				frontmatter: frontmatterRecord,
 			});
 		} catch (error) {
 			tasknotesLogger.error(`Error extracting task info from native metadata for ${path}:`, {
diff --git a/src/utils/clickHandlers.ts b/src/utils/clickHandlers.ts
index 37da3ef..54c50be 100644
--- a/src/utils/clickHandlers.ts
+++ b/src/utils/clickHandlers.ts
@@ -1,6 +1,7 @@
-import { TFile } from "obsidian";
+import { Notice, TFile } from "obsidian";
 import { TaskInfo } from "../types";
 import TaskNotesPlugin from "../main";
+import { getTaskSourceOrigin, openTaskSourceOrigin } from "./sourceOrigin";
 
 interface BatchContextMenuAdapter {
 	show(event: MouseEvent): void;
@@ -33,10 +34,25 @@ const DEFAULT_EXCLUDE_SELECTOR = [
 	".tag",
 ].join(", ");
 
+function clearPendingClickTimeout(timeout: number | null): null {
+	if (timeout) {
+		window.clearTimeout(timeout);
+	}
+	return null;
+}
+
 /**
  * Creates a reusable click handler that supports single/double click distinction
  * based on user settings.
- * Ctrl/Cmd + Click: Opens source note immediately
+ *
+ * Modifier shortcuts (independent of single/double-click settings):
+ * - Ctrl/Cmd + Click: open the task note in a new tab
+ * - Alt + Click: open the originating source note (when present)
+ * - Middle click: open the task note in a new tab
+ *
+ * Attach `clickHandler` with `{ capture: true }` so excluded interactive controls
+ * (Source pill, status dots, etc.) can cancel a pending single-click timer even
+ * when they stopPropagation on the bubble phase.
  */
 export function createTaskClickHandler(options: ClickHandlerOptions) {
 	const {
@@ -69,12 +85,28 @@ export function createTaskClickHandler(options: ClickHandlerOptions) {
 		await plugin.openTaskEditModal(task);
 	};
 
+	const openSourceNote = async (newTab = false): Promise<boolean> => {
+		if (!getTaskSourceOrigin(task)) {
+			return false;
+		}
+		return openTaskSourceOrigin(plugin, task, { newLeaf: newTab });
+	};
+
 	const handleSingleClick = async (e: MouseEvent) => {
 		if (onSingleClick) {
 			await onSingleClick(e);
 			return;
 		}
 
+		// Alt+Click: open originating source note (unused modifier; leaves settings alone)
+		if (e.altKey) {
+			const opened = await openSourceNote(e.ctrlKey || e.metaKey);
+			if (!opened) {
+				new Notice(plugin.i18n.translate("ui.taskCard.sourceBadgeMissing"));
+			}
+			return;
+		}
+
 		if (e.ctrlKey || e.metaKey) {
 			openNote(true); // Open in new tab
 			return;
@@ -105,6 +137,8 @@ export function createTaskClickHandler(options: ClickHandlerOptions) {
 	const handleClick = async (e: MouseEvent) => {
 		const target = e.target as HTMLElement;
 		if (target.closest(clickExcludeSelector)) {
+			// Cancel a pending card single-click so Source/status controls never race the edit modal
+			clickTimeout = clearPendingClickTimeout(clickTimeout);
 			return;
 		}
 
@@ -113,6 +147,7 @@ export function createTaskClickHandler(options: ClickHandlerOptions) {
 		if (selectionService) {
 			if (e.shiftKey) {
 				e.stopPropagation();
+				clickTimeout = clearPendingClickTimeout(clickTimeout);
 
 				// Enter selection mode if not already active
 				if (!selectionService.isSelectionModeActive()) {
@@ -134,14 +169,20 @@ export function createTaskClickHandler(options: ClickHandlerOptions) {
 		// Stop propagation to prevent clicks from bubbling to parent cards
 		e.stopPropagation();
 
+		// Alt+Click should open source immediately (no double-click delay)
+		if (e.altKey) {
+			clickTimeout = clearPendingClickTimeout(clickTimeout);
+			await handleSingleClick(e);
+			return;
+		}
+
 		if (plugin.settings.doubleClickAction === "none") {
 			await handleSingleClick(e);
 			return;
 		}
 
 		if (clickTimeout) {
-			window.clearTimeout(clickTimeout);
-			clickTimeout = null;
+			clickTimeout = clearPendingClickTimeout(clickTimeout);
 			await handleDoubleClick(e);
 		} else {
 			clickTimeout = window.setTimeout(() => {
@@ -162,15 +203,23 @@ export function createTaskClickHandler(options: ClickHandlerOptions) {
 
 		const target = e.target as HTMLElement;
 		if (target.closest(clickExcludeSelector)) {
+			clickTimeout = clearPendingClickTimeout(clickTimeout);
 			return;
 		}
 
 		e.preventDefault();
 		e.stopPropagation();
-		if (clickTimeout) {
-			window.clearTimeout(clickTimeout);
-			clickTimeout = null;
+		clickTimeout = clearPendingClickTimeout(clickTimeout);
+
+		if (e.altKey) {
+			void openSourceNote(true).then((opened) => {
+				if (!opened) {
+					new Notice(plugin.i18n.translate("ui.taskCard.sourceBadgeMissing"));
+				}
+			});
+			return;
 		}
+
 		openNote(true);
 	};
 
@@ -300,6 +349,16 @@ export async function handleCalendarTaskClick(
 	};
 
 	const handleSingleClick = async (e: MouseEvent) => {
+		if (e.altKey) {
+			const opened = await openTaskSourceOrigin(plugin, task, {
+				newLeaf: e.ctrlKey || e.metaKey,
+			});
+			if (!opened) {
+				new Notice(plugin.i18n.translate("ui.taskCard.sourceBadgeMissing"));
+			}
+			return;
+		}
+
 		if (e.ctrlKey || e.metaKey) {
 			openNote(true); // Open in new tab
 			return;
diff --git a/src/utils/taskInfoAssembly.ts b/src/utils/taskInfoAssembly.ts
index 383ef28..84a9602 100644
--- a/src/utils/taskInfoAssembly.ts
+++ b/src/utils/taskInfoAssembly.ts
@@ -1,5 +1,6 @@
 import type { TaskInfo } from "../types";
 import { calculateTotalTimeSpent } from "./helpers";
+import { hydrateTaskSourceOrigin } from "./sourceOrigin";
 
 export interface BuildTaskInfoFromMappedTaskInput {
 	path: string;
@@ -7,6 +8,7 @@ export interface BuildTaskInfoFromMappedTaskInput {
 	defaultTaskStatus: string;
 	isBlocked: boolean;
 	blockingTasks: string[];
+	frontmatter?: Record<string, unknown> | null;
 }
 
 export function buildTaskInfoFromMappedTask({
@@ -15,12 +17,13 @@ export function buildTaskInfoFromMappedTask({
 	defaultTaskStatus,
 	isBlocked,
 	blockingTasks,
+	frontmatter,
 }: BuildTaskInfoFromMappedTaskInput): TaskInfo {
 	const totalTrackedTime = mappedTask.timeEntries
 		? calculateTotalTimeSpent(mappedTask.timeEntries)
 		: 0;
 
-	return {
+	const assembled: TaskInfo = {
 		...mappedTask,
 		id: path,
 		path,
@@ -36,4 +39,6 @@ export function buildTaskInfoFromMappedTask({
 		isBlocking: blockingTasks.length > 0,
 		blocking: blockingTasks.length > 0 ? blockingTasks : undefined,
 	};
+
+	return hydrateTaskSourceOrigin(assembled, frontmatter ?? mappedTask.customProperties);
 }
diff --git a/styles/task-card-bem.css b/styles/task-card-bem.css
index cabc382..dc7a438 100644
--- a/styles/task-card-bem.css
+++ b/styles/task-card-bem.css
@@ -538,6 +538,24 @@ body.is-mobile .tasknotes-plugin .task-card.task-card--drag-handle-only .task-ca
     height: 12px;
 }
 
+.tasknotes-plugin .task-card__metadata-pill--source {
+    background: color-mix(in srgb, var(--text-muted) 8%, transparent);
+    color: var(--text-muted);
+    border-color: color-mix(in srgb, var(--text-muted) 30%, var(--background-modifier-border));
+    cursor: pointer;
+}
+
+.tasknotes-plugin .task-card__metadata-pill--source:hover {
+    color: var(--text-accent);
+    border-color: color-mix(in srgb, var(--interactive-accent) 35%, var(--background-modifier-border));
+    background: color-mix(in srgb, var(--interactive-accent) 10%, transparent);
+}
+
+.tasknotes-plugin .task-card__metadata-pill--source svg {
+    width: 12px;
+    height: 12px;
+}
+
 /* Reminder indicator */
 .tasknotes-plugin .task-card__reminder-indicator {
     width: 14px;
diff --git a/tests/unit/ui/taskCardMetadata.test.ts b/tests/unit/ui/taskCardMetadata.test.ts
index f1329e2..4402f25 100644
--- a/tests/unit/ui/taskCardMetadata.test.ts
+++ b/tests/unit/ui/taskCardMetadata.test.ts
@@ -30,9 +30,13 @@ function createPlugin(): TaskNotesPlugin {
 			},
 			vault: {
 				getAbstractFileByPath: jest.fn(() => null),
+				read: jest.fn().mockResolvedValue(""),
 			},
 			workspace: {
 				openLinkText: jest.fn(),
+				getLeaf: jest.fn(() => ({
+					openFile: jest.fn().mockResolvedValue(undefined),
+				})),
 			},
 		},
 		fieldMapper: {
@@ -48,11 +52,20 @@ function createPlugin(): TaskNotesPlugin {
 					"ui.taskCard.blockingBadge": "Blocking",
 					"ui.taskCard.blockingBadgeTooltip": "This task is blocking another task",
 					"ui.taskCard.googleCalendarSyncTooltip": "Synced to Google Calendar",
+					"ui.taskCard.sourceBadge": "Source",
+					"ui.taskCard.sourceBadgeAria": "Open source note {name}",
+					"ui.taskCard.sourceBadgeTooltip": "Open originating note: {path}",
 					"ui.taskCard.labels.due": "Due",
 					"ui.taskCard.labels.scheduled": "Scheduled",
 				};
 				if (translations[key]) {
-					return translations[key];
+					let text = translations[key];
+					if (vars) {
+						for (const [name, value] of Object.entries(vars)) {
+							text = text.replace(`{${name}}`, String(value));
+						}
+					}
+					return text;
 				}
 				if (vars) {
 					return `${key} ${Object.values(vars).join(" ")}`;
@@ -308,6 +321,48 @@ describe("taskCardMetadata", () => {
 		expect(parentClick).not.toHaveBeenCalled();
 	});
 
+	it("renders source origin as an interactive pill that does not open the card", async () => {
+		const sourceFile = { path: "Notes/Origin.md" };
+		const openFile = jest.fn().mockResolvedValue(undefined);
+		const plugin = createPlugin();
+		(plugin.app.vault.getAbstractFileByPath as jest.Mock).mockImplementation((path: string) =>
+			path === "Notes/Origin.md" ? sourceFile : null
+		);
+		(plugin.app.vault.read as jest.Mock).mockResolvedValue("- [[task]] from convert\n");
+		(plugin.app.workspace.getLeaf as jest.Mock).mockReturnValue({ openFile });
+
+		const { card, metadataLine } = createMetadataHost();
+		const parentClick = jest.fn();
+		card.addEventListener("click", parentClick);
+
+		const elements = renderTaskCardMetadata({
+			metadataLine,
+			card,
+			task: createTask({
+				sourceNote: "Notes/Origin.md",
+				sourceLine: 0,
+			}),
+			plugin,
+			visibleProperties: ["due"],
+			onBlockedByToggle: jest.fn(),
+		});
+
+		const sourcePill = metadataLine.querySelector<HTMLElement>(
+			".task-card__metadata-pill--source"
+		);
+		expect(elements).toEqual([sourcePill]);
+		expect(sourcePill?.textContent).toContain("Source: Origin");
+		expect(sourcePill?.getAttribute("role")).toBe("button");
+
+		sourcePill?.click();
+		await Promise.resolve();
+		await Promise.resolve();
+
+		expect(parentClick).not.toHaveBeenCalled();
+		expect(plugin.app.workspace.getLeaf).toHaveBeenCalledWith(false);
+		expect(openFile).toHaveBeenCalledWith(sourceFile, { eState: { line: 0 } });
+	});
+
 	it("clears stale metadata and hides the line when no configured property renders", () => {
 		const plugin = createPlugin();
 		const { card, metadataLine } = createMetadataHost();
